# Optional: forward contact-form leads to any webhook collector
# (Zapier, Make, HubSpot, or the future CRM's intake endpoint).
# Leave unset to only log leads to the server console / Vercel function logs.
CRM_WEBHOOK_URL=
